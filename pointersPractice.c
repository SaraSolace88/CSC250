#include <stdio.h>
#include <stdlib.h>



foo(int arrf[10], int maxf, int sumf, int productf){

}


int main(){
    int max;
    int sum;
    int product;
    int arr[10];
    foo(arr, &max, &sum, &product);
    printf("%d %d %d\n", max, sum, product);
}
