#include <stdio.h>
#include <stdlib.h>
#include <time.h>

void print_rand()[
    int x;
    x = rand() % 10 + 1;
    printf("%d\n", x);
    return;
]

int sum_numbers(int x, int y); //makes it so you can put functions under main


int main(){
    srand(time(NULL));
    int sum;
    sum
    return 0;
}

int sum_numbers(int x, int y)[
    int sum;
    sum = x + y;
    return sum;
    
]
